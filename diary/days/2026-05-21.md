# 2026-05-21

## Changes
- Created durable project memory files: `AGENTS.md`, `Summarizations.md`, and `diary/`.
- Built the initial Russian touch typing trainer prototype as a Vite + React + TypeScript app with dashboard, lessons, and trainer screens.
- Imported the 30-lesson Russian touch typing course from `/Users/seva/Downloads/kurs_slepoj_pechati_ru_30_urokov.md` into `src/data/lessons.ts`.
- Made lesson routing dynamic so lessons beyond the MVP `/lessons/7` can start.
- Replaced the old Magic Keyboard implementation with an Aceternity/shadcn-style keyboard component using `src/components/ui/keyboard.tsx`, `src/lib/utils.ts`, `components.json`, Tailwind/Vite config changes, and sound files in `public/sounds/`.
- Tuned the trainer keyboard: Russian key labels, light body, large `zoom: 2.31` scale, smaller body corner radius, custom sound toggle integration, soft pressed-state, and restored hints/current/error highlighting.
- Fixed typing behavior: fast typing index uses a ref, `-` is accepted for dash variants, typing text wraps whole words, text size was reduced, and caret became a stable purple transform-moving element with lazy blink.
- Updated durable memory to reflect that the current approved keyboard is Aceternity-style, not the previous Magic Keyboard.
- Replaced the temporary global site shrink (`--site-scale` + `.app-shell transform`) with real smaller CSS dimensions across layout, typography, cards, controls, charts, trainer text, and keyboard containers.
- Cleaned confirmed dead/prototype code: old keyboard CSS systems, eye-tracking product UI, `CurrentSessionCard`, `TrainerMetricsBar`, `keyboard-demo`, and `Chip`.
- Kept the Aceternity keyboard fit adapter and increased the final desktop keyboard cap to `2.15` after user feedback that it became too small.
- Fixed the keyboard hide/show path so hidden state uses explicit `onHide/onShow` and `.keyboard-deck-wrap.is-hidden` actually collapses the keyboard deck.
- Renamed the first course section to `Начальный уровень` and made the lessons screen section-based with collapsible levels.
- Added `Базовый уровень` as the second section with lessons 31-60 from `/Users/seva/Downloads/bazovy_uroven_slepaya_pechat_uroki_31_60_s_metadannymi.md`, preserving titles, texts, duration ranges, row counts, and difficulty.
- Updated lesson preview behavior so hover/focus on any lesson changes the right-side information block; the preview card stays sticky on desktop.
- Reworked lesson metadata so the first 30 lessons use corrected `duration`/`textsCount`, the full course now reports 60 lessons, and course total time is computed from lesson metadata.
- Implemented, then removed, a visual hand/finger overlay for hints after user rejection. The key-to-finger mapping remains in `getActiveFingers()` for future hint UI work.

## Verification
- `npm run build` passed after the latest caret blink change.
- `npm run build` passed again after the 15% real-size cleanup and after the keyboard hide/show fix.
- `npm run build` passed after adding lessons 31-60 and again after removing the visual finger overlay.
- `npm exec -- tsx ...` verified 60 lessons, section ranges 1-30 and 31-60, and `courseStats.totalLessons = 60`.
- `rg -n "WPM|wpm" src` and the camera/heatmap/eye-tracking grep returned no matches after cleanup.
- `rg -n "finger-guide|keyboard-finger-guide|FingerGuide|FINGER_LABELS" src` returned no matches after finger visual removal.
- `curl -I` checks for `/lessons` and `/lessons/1` returned `200 OK`.
- Browser QA covered `/`, `/lessons`, `/lessons/7`, and `/lessons/12` across desktop viewports plus a small fallback. The final keyboard hide CSS fix built successfully, but the last browser recheck timed out and should be manually verified next.
- Browser preview was restarted at `http://127.0.0.1:4174/`.
- Browser checks during the session covered `/lessons/12`, keyboard rendering, sound assets, hints toggling, Russian labels, and typing/caret behavior.
- One browser automation attempt to type text hit a virtual clipboard limitation; subsequent keypress/screenshot checks verified caret movement.

## Decisions
- See `D-20260521-001` through `D-20260521-006` in `diary/decisions.md`.

## Carry Forward
- Continue from `Summarizations.md` in the next session.
- Browser-check `/lessons`: section collapse/expand, hover preview for lessons 31-60, and sticky preview behavior.
- Browser-check `/lessons/31` and `/lessons/60`: target keys, typing text, hints, keyboard hide/show, Backspace, pause/resume.
- When designing the next finger-hint visual, reuse `getActiveFingers()` in `src/components/trainer/RussianKeyboard.tsx`.
