# 2026-05-21

## Changes
- Created durable project memory files: `AGENTS.md`, `Summarizations.md`, and `diary/`.
- Captured the current implementation state after building the Russian touch typing trainer prototype.
- Recorded that `/lessons/7` is the main active lesson/trainer screen with a refined typing card, smooth caret, Russian keyboard, and compact side controls.
- Refined `/lessons/7` into a cleaner white active lesson screen: removed the duplicated upper metrics panel, moved pause/resume into the header, enlarged the typing card, and made the lower session card the single metrics surface.
- Restyled the virtual keyboard toward a large Apple Magic Keyboard-like visual: light tray, larger physical keycaps, purple `Н`/`Р`, light lavender spacebar without text/pulse, and purple arrows.

## Verification
- `npm run build` passed after the UI work.
- Browser QA was run on `/lessons/7` with Playwright/Chrome for desktop, tablet, and mobile fallback.
- Verified interactions included correct typing, error behavior without caret movement, pause/resume, keyboard hide/show persistence, and compact toggles.
- Later browser QA on `/lessons/7` verified no upper stats bar, header pause, white main/sidebar surfaces, keyboard visible/collapsed state, no `WPM`, no console error-level messages, and no horizontal page overflow.

## Decisions
- See `D-20260521-001`, `D-20260521-002`, and `D-20260521-003` in `diary/decisions.md`.

## Carry Forward
- Define a minimum supported macOS-style app window size before further desktop polish.
- Full requested keyboard dimensions do not fit the current central column without changing page composition; decide whether to keep tuning inside the existing layout or widen the active lesson content area.
- Real lesson content, persistent statistics, real keyboard sounds, and real eye tracking remain future implementation phases.
